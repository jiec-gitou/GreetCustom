<server>

    <featureManager>
        <feature>servlet-3.1</feature>
    	<feature>webProfile-7.0</feature>
	</featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

    <application name="GreetCustom" context-root="/"
    	location="${appLocation}" type="war">
    </application>

</server>
